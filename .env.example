# Uptrack Environment Variables Example
# Copy this file to .env and fill in your values for production deployment

# ============================================================================
# DATABASE CONFIGURATION - Single Database, Multiple Repos for Pooling
# ============================================================================

# PostgreSQL - Shared database for app data and Oban job queue
# AppRepo handles all migrations (app schema + oban schema)
# ObanRepo uses separate connection pool to isolate job processing
DATABASE_URL=postgresql://uptrack:PASSWORD@DATABASE_HOST:5432/uptrack_prod

# ============================================================================
# CONNECTION POOL CONFIGURATION - Separate Pools per Repo
# ============================================================================
# Two repos with separate connection pools prevent job queue from
# starving application queries:
#
# APP_POOL_SIZE: Application queries (users, monitors, incidents, etc.)
#   - Typical range: 8-15 connections
#   - Germany primary: 15, India replica: 10
#
# OBAN_POOL_SIZE: Job queue (monitor checks, webhooks, etc.)
#   - Typical range: 20-40 connections
#   - Germany primary: 30, India replica: 25

APP_POOL_SIZE=10
OBAN_POOL_SIZE=20

# ============================================================================
# ECTO CONFIGURATION
# ============================================================================
# IPv6 support (set to "true" or "1" to enable)
ECTO_IPV6=false

# ============================================================================
# OBAN JOB QUEUE CONFIGURATION
# ============================================================================

# Node name for Oban cluster identification
OBAN_NODE_NAME=uptrack-node-1

# Job concurrency per queue
OBAN_CHECKS_CONCURRENCY=50
OBAN_WEBHOOKS_CONCURRENCY=10
OBAN_INCIDENTS_CONCURRENCY=5

# ============================================================================
# VICTORIAMETRICS CONFIGURATION - Time-Series Database
# ============================================================================
# VictoriaMetrics cluster is used for time-series monitoring data
# Separate from PostgreSQL for optimal performance
#
# Cluster components:
#  - vminsert: Accepts incoming metrics (write path)
#  - vmstorage: Stores time-series data
#  - vmselect: Handles queries (read path)

VICTORIAMETRICS_VMINSERT_URL=http://vminsert.internal:8480
VICTORIAMETRICS_VMSELECT_URL=http://vmselect.internal:8481

# ============================================================================
# PHOENIX ENDPOINT CONFIGURATION
# ============================================================================

# Server settings
PHX_HOST=example.com
PHX_SERVER=true
PORT=4000

# Secret key for signing cookies and tokens
# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=your_secret_key_base_here

# ============================================================================
# MONITORING NODE INFORMATION
# ============================================================================

# Geographic region for this node
NODE_REGION=eu-central

# Node name/identifier
NODE_NAME=germany

# ============================================================================
# DNS CLUSTER CONFIGURATION
# ============================================================================
# Optional: For cluster discovery via DNS
DNS_CLUSTER_QUERY=uptrack.local

# ============================================================================
# OBSERVABILITY (optional but recommended)
# ============================================================================

# AppSignal APM — set to enable performance metrics. Leave unset to disable.
# Get a key at https://appsignal.com after creating a project.
APPSIGNAL_PUSH_API_KEY=

# Sentry error tracking — create a project at https://sentry.io
SENTRY_DSN=

# ============================================================================
# PADDLE BILLING (optional — required only if you enable billing)
# ============================================================================
# Sandbox account: https://sandbox-vendors.paddle.com/signup
# Production: https://vendors.paddle.com
# All values must be supplied. Leave the section unset to disable billing.

PADDLE_API_KEY=
PADDLE_WEBHOOK_SECRET=
PADDLE_BASE_URL=https://sandbox-api.paddle.com
PADDLE_CHECKOUT_URL=https://sandbox-checkout.paddle.com
PADDLE_PRICE_ID_PRO=
PADDLE_PRICE_ID_PRO_ANNUAL=
PADDLE_PRICE_ID_TEAM=
PADDLE_PRICE_ID_TEAM_ANNUAL=

# ============================================================================
# OAUTH (optional — required only if you enable social login)
# ============================================================================
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================================================
# NOTES FOR PRODUCTION DEPLOYMENT
# ============================================================================
#
# Germany Node (PostgreSQL PRIMARY):
#   export APP_POOL_SIZE=15
#   export OBAN_POOL_SIZE=30
#   export RESULTS_POOL_SIZE=20
#   export OBAN_NODE_NAME=germany
#   export NODE_REGION=eu-central
#
# Canada Node (App-only):
#   export APP_POOL_SIZE=8
#   export OBAN_POOL_SIZE=20
#   export RESULTS_POOL_SIZE=12
#   export OBAN_NODE_NAME=canada
#   export NODE_REGION=na-east
#
# Use Tailscale for private inter-node communication
# Use SSL certificates for external HTTPS
